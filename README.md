# Mask detection model
![mask-detection](static/mask.jpg)  
This project aims to compare various methods of creating a mask detection model.
## Face detection
As for now, I focused on choosing the best face detector that will extract faces for the mask detection. The comparison is performed in the `detector_choice.ipynb`.